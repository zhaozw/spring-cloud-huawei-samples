<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huaweicloud.samples.dao.AccountDao">
    <update id="decrease">
        update t_account set residue = residue- #{money},used = used + #{money}
        where user_id =#{userId}
    </update>

    <select id="getAccountByUserId" resultType="com.huaweicloud.samples.domain.Account" parameterType="long">
        select * from t_account
        where user_id=#{userId};
    </select>
</mapper>
